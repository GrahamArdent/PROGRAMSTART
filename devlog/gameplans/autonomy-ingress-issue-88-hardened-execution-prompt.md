# PROGRAMSTART Execution Prompt — Reconcile and Close Autonomy Issue #88

## Durable Resume Contract
**Continue Autonomy Ingress Closure**

## Mode
Existing ecosystem / Mode C. Use current PROGRAMSTART methodology, Current Search, current owning authority, Challenge, acceptance, and Learning.

This prompt is a starting objective, not durable authority. Re-read current repository/runtime truth before every consequential action. Do not create a second autonomous ecosystem.

## Primary Objective
Resolve the autonomy defect exposed by the untouched PATH-WP1 / PATH-03 handoff acceptance test so that a concise natural operator objective can enter through the intended operator surface, be converted through the existing PROGRAMSTART Intent Ingress boundary into trusted typed semantics and current authority, reach Controller admission/continuation, and then continue durably without ChatGPT or Graham repeatedly acting as command transport, semantic harvester, prompt reseeder, state synchronizer, or nervous system.

The goal is not merely to make issue #88 green. Prove the existing autonomy backbone can ingest and continue the original PATH-WP1 workload end to end.

## Required Starting Reconciliation
Before implementation, run Current Search / Mode-C orientation across:
- `GrahamArdent/PROGRAMSTART`: current methodology; ADR 0025; Intent Ingress V0.1; Contextual Proceed V0.2; Work Packet semantics; current Intent Ingress code/tests; newer superseding authority.
- `GrahamArdent/mission-control`: `AGENTS.md`; `MISSION_CONTROL_EXECUTION.md`; current packet/closure receipt; MC-05 governed objective intake; any current/live objective transport or newer accepted boundary.
- `GrahamArdent/programstart-autonomous-controller`: issue #88 including reconciliation correction; contextual/private ingress; semantic admission/continuation; AC-09/current owner-handoff machinery; colliding active work.
- Compute Spine, Execution Node, Evidence Spine, Watchtower, ecosystem-contracts/Path Authority and owning-project state only where materially relevant.

Search for equivalent active work before creating a packet/PR. Reuse/reconcile rather than duplicate.

## Canonical Architecture Boundary to Preserve
Verify against current authority:
`operator -> Mission-Control bounded objective/raw-intent intake -> PROGRAMSTART Intent Ingress / trusted semantic interpretation -> current authority/currentness -> sealed Work Packet -> Autonomous Controller admission/continuation -> Compute Spine -> bounded worker/executor -> evidence -> operator surface`

- Mission-Control is human/operator injection/presentation. It may capture concise raw intent and mechanically known metadata. It MUST NOT become a second semantic compiler, Controller, Work Packet authority, or durable workflow engine.
- PROGRAMSTART Intent Ingress owns reusable intent interpretation/transition/compilation semantics. Preserve the deterministic compiler boundary.
- Autonomous Controller owns durable semantic admission, continuation, waits/resume, retry/recovery, owner handoff and genuine human gates. Do not move raw-language interpretation into Controller merely because the failed test entered there.
- Compute Spine owns execution-fabric mechanics, not semantic intent.
- Evidence/current-evidence owners own provenance/currentness references, not execution authority.
- Owning repositories remain authoritative for project truth/consequences.

If fresher canonical authority contradicts this split, reconcile it explicitly before implementation.

## Preserve the Failure as Evidence
The original request reached real Controller private ingress over an admitted private path but failed closed because the natural objective was not the typed trusted `ConversationHarvest` required by contextual runtime.

Transport into Controller was not the primary failure.

Do not erase this evidence by manually completing PATH-WP1. PATH-WP1 / PATH-03 remains the untouched acceptance workload until the repaired autonomy path itself takes it over.

## Design Principle — Simplify Before Adding Intelligence
Do not put the entire request/ecosystem/authority into one giant LLM call returning JSON.

Classify each required field first:

### 1. Mechanically known facts
Authenticated/source principal; exact raw utterance; timestamp; context/source ref; active objective/run/packet refs when known; durable artifact/repository refs; transport/source identity.

Structure directly. Do not ask a model to rediscover them.

### 2. Existing typed/deterministic semantics
Reuse PROGRAMSTART continuation/transition semantics, packet/currentness/gate/owner-handoff contracts, and exact structured refs.

### 3. Actually unresolved semantic meaning
Use the smallest bounded trusted semantic interpretation necessary. Preserve producer/version/provenance. Expose ambiguity. Do not infer authority from confidence.

### 4. Authority/currentness/effects
Resolve independently from current durable owners/evidence. Probabilistic interpretation MUST NOT manufacture authorization, permission, spend, credential/destructive authority, or human-gate approval.

Prefer:
`known facts -> deterministic normalization -> minimal semantic inference -> deterministic validation/currentness -> existing typed harvest/Work Packet -> Controller`

Do not introduce a specialized classifier/model unless measured need earns it.

## Required Investigation Before Coding
Determine with evidence:
1. Exact information accepted Mission-Control objective intake already captures.
2. Exact raw-intent/metadata contract PROGRAMSTART Intent Ingress accepts.
3. ConversationHarvest fields that can be supplied mechanically.
4. Fields genuinely requiring semantic interpretation.
5. Whether an existing trusted semantic producer already exists and was bypassed.
6. Whether authority/currentness resolution is already machine-accessible and should be wired rather than rebuilt.
7. Whether #88 belongs in Controller after reconciliation or should split/move to PROGRAMSTART plus minimal Mission-Control integration.
8. Whether live Mission-Control/provider gates prevent physical Mission-Control replay; if so, use the smallest owner-faithful bounded equivalent without pretending production UI is live.
9. Exact durable auto-resume mechanism after temporary automation/runtime failure versus a genuine human gate.

Do not implement until these yield a coherent smallest-change design.

## Implementation Constraints
The solution MUST:
- reuse existing PROGRAMSTART Intent Ingress and Work Packet contracts;
- preserve Controller as durable semantic continuation owner;
- preserve Mission-Control as replaceable operator surface;
- preserve raw intent and provenance for interpreted material;
- fail closed on material ambiguity;
- distinguish automation gaps from genuine human gates;
- revalidate current authority before mutation;
- be replay/idempotency safe;
- survive interruption at the appropriate owner boundary;
- avoid asking the operator to reconstruct machine-recoverable context;
- avoid ChatGPT as required runtime infrastructure;
- avoid manual relay of CI/evidence/commands/state/retries;
- avoid a second queue/controller/state machine/prompt service/compiler/global backlog/authority store;
- avoid widening public ingress, secrets, identity, provider permissions, spend or destructive authority absent a separately owned gate.

If multiple repos are required, derive the smallest owner-specific packets and use existing owner-handoff/Controller machinery. Do not use chat as cross-repository scheduler.

## Mandatory PROGRAMSTART Challenge
Challenge before design acceptance and again against exact implementation heads. Attack at least:
1. prose authority smuggling;
2. stale conversation vs current authority;
3. ambiguous “proceed/continue” referents;
4. context truncation losing accepted constraints;
5. known facts unnecessarily sent through probabilistic inference;
6. interpreter unavailable/malformed/low-confidence;
7. wrong but syntactically valid inference;
8. duplicate/replayed objective;
9. restart after interpretation before Controller admission;
10. restart after Controller admission;
11. resolved target owner differs from assumption;
12. active genuine human gate;
13. already-complete work;
14. authority changes after interpretation;
15. Mission-Control becomes second compiler/orchestrator;
16. Controller absorbs natural-language interpretation;
17. contextual prose side-channels around sealed packet;
18. confidence score mistaken for authorization;
19. transport failure during handoff;
20. manual PATH-WP1 completion falsely counted as autonomy success.

Remediate material findings and re-Challenge.

## End-to-End Acceptance
After owner-normal repository/runtime acceptance, replay the original untouched PATH-WP1 / PATH-03 autonomy handoff from concise natural intent through the intended Mission-Control/PROGRAMSTART boundary, or the smallest explicitly equivalent owner-faithful live harness if Mission-Control production deployment remains separately gated.

Then stop manually steering.

Success requires the ecosystem itself to:
1. capture objective/known context;
2. produce/recover trusted typed semantic harvest;
3. resolve current authority/currentness;
4. reuse/recompile correct sealed Work Packet;
5. obtain Controller admission/continuation;
6. determine next authorized action;
7. execute through existing owner/Compute/worker paths;
8. handle dependencies/retries/temporary automation gaps/owner handoffs without chat reseeding;
9. persist state/evidence;
10. preserve genuine human gates exactly;
11. continue to truthful terminal state or genuine nondelegable gate.

PATH-WP1 finishing is insufficient if ChatGPT/operator manually performs its steps.

## PATH-WP1 Replay Safety
Re-read current Path Authority. Preserve all still-current invariants for human EN access, independent Compute→EN recovery, preferred normal machine path, Git compatibility/fallback, identity separation, same-effect/replay safety, recovery/re-proof/return-to-primary.

Do not hard-code historical ports, commits, identities or PR numbers as current truth.

## Genuine Human Gates
If reached: minimize to exact nondelegable action; persist consequence/authority/invalidation/resume semantics; stop truthfully; auto-resume afterward where authorized.

Missing automation, semantic machinery, currentness retrieval or implementation defects are NOT human gates.

## Durable Reconciliation
At closure:
- reconcile #88 to correct owner/disposition;
- update owner-specific authority/acceptance only where earned;
- preserve original failed handoff;
- record exact implementation/CI/Challenge/runtime evidence;
- route Learning to behavior owner;
- remove/replace stale current-work pointers;
- never leave completed work described as active.

## Final Acceptance Report
Report durable references for:
- final owner split;
- reused existing capability;
- minimum new capability;
- mechanical vs semantically inferred vs authority-resolved data;
- Challenge findings/remediations;
- what backbone completed itself;
- intervention needed only to repair autonomy machinery;
- genuine human gates;
- PATH-WP1 autonomous terminal/gated outcome;
- whether this class of natural handoff now succeeds without ChatGPT as nervous system.

## Stop Conditions
Stop only at:
1. **ACCEPTED** — end-to-end autonomy proven;
2. **GENUINE HUMAN GATE** — exact nondelegable consequence durably persisted;
3. **SAFE BLOCKER** — machine-remediable autonomy defect cannot be repaired within current authority; persist exact owner/blocker/evidence/smallest next owner action.

Do not stop merely because one PR merged, CI turned green, one packet completed, or an intermediate transport worked.
